# 10 测试与验收

## 已验证

- 固定 CSV 身份、字段、缺失、时间和只读性；
- 短/长缺失规则、15 分钟聚合和固定切分；
- manifest、Parquet 和 SQLite 轻量元数据；
- 历史 KPI、趋势、周期和分项分析；
- 672→96 窗口、缩放器、六模型比较和指标；
- LSTM/PatchTST CPU/CUDA 训练、保存加载与复现；
- 90% 共形区间；
- 冻结预测、缓存和 CSV；
- 历史回放、三类预警和预警 CSV；
- 七页应用启动与关键 AppTest；
- 智能建议本地模板、单次 Chat Completions mock 和失败回退；
- Ruff、格式、mypy、pytest、pip check 和 pre-commit。
- 七页 1920×1080 最终浏览器验收和等效 125% 缩放检查；
- 15 页 A4 Word/PDF 报告的目录、页码、图表和逐页渲染检查。

精简后完整回归为 128 项 pytest。

## 正式事实

| 项目 | 结果 |
| --- | --- |
| 15 分钟点数 | 17,376 |
| 有效点 | 17,127 |
| 覆盖率 | 98.567% |
| 累计电量 | 4,988.285 kWh |
| 默认模型 | 前一日同刻 |
| 默认测试 MAE | 0.6463 kW |
| PatchTST 测试 MAE | 0.6622 kW |
| 预警验收 | 4 质量 + 6 规则 + 13 残差 |

## API 验收

自动测试不得访问网络，使用 mock 验证：

- 只调用一次 `chat.completions.create`；
- 请求包含聚合 JSON；
- 不包含原始 CSV 或完整时序；
- 空响应和异常回退本地模板；
- 未配置 Key 时页面不显示调用按钮；
- 页面加载不发起请求。

真实 API 调用是可选人工验收，不阻塞课程核心功能。
本次最终环境未安全配置 API，真实调用按规则跳过；未要求或记录任何 Key。

## 最终交付状态

- 七页最终截图已生成，分辨率均为 1920×1080；
- 七页在等效 125% 缩放下无横向溢出；
- Word/PDF 均可打开，正文页码从 1 开始，目录和图表编号已更新；
- 完整 pytest 为 128 项通过，其他质量门禁全部通过；
- 尚需用户填写封面个人信息与实际小组分工，并在提交前复核 10 分钟演示流程；
- 可选真实 API 测试仅在用户自己的环境安全配置完成后执行。

已删除功能不进入测试矩阵。
